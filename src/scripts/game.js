function Game() {

    var prepareField = function() {
        

        
        // for (var i = 0; i < 8; i += 2) {
        //     for (var j = 0; j < 8; j += 2) {
        //         ctx.clearRect(20 + i * 32, 20 + j * 32, 32, 32);
        //         ctx.clearRect(20 + (i + 1) * 32, 20 + (j + 1) * 32, 32, 32);
        //     }
        // }
    }

    this.start = function() {
        prepareField();
    }  

}

